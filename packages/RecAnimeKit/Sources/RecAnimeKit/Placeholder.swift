import Foundation
import RecAnimeCore

/// Networking and stores are added in the Core/Kit milestone. This file keeps the target buildable.
public enum RecAnimeKitVersion {
    public static let current = "0.1.0"
}
